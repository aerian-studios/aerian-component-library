import React from "react";
import cx from "classnames";
import styles from "./{{pascalCase name}}.module.scss";
import { DefaultProps } from "../../types/types";


export interface {{pascalCase name}}Props extends DefaultProps<"div"> {}
/**
 * IMPORTANT: Add a description to document this component for storybooks
 */
export const {{pascalCase name}} = ({
    className,
    ...rest
}: {{pascalCase name}}Props) => {
    return <div className={cx(styles.component, className)} {...rest}></div>;
};
